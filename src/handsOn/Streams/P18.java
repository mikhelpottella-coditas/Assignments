import static java.util.Arrays.asList;

void main() {
//     Find all pairs of numbers from a list that add up to a given sum.
    List<Integer> numbers = new ArrayList<>(asList(1, 2, 3, 4, 5));
    var target = 6;
}